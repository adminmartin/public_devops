pipeline {
  environment {
    registry = "martindevopsdocker/martindockerhub"
    registryCredential = 'dockerhub'
    dockerImage = ''
  }
    agent { dockerfile true }
      stages{
        stage('Build image') {
            steps {
		script {
                dockerImage = docker.build("apache-image:${env.BUILD_ID}")
            }
	}
        }
    stage('Deploy Image') {
      steps{
        script {
          docker.withRegistry( '', registryCredential ) {
            dockerImage.push()
          }
        }
      }
}
}
}
